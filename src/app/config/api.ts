import { environment } from './../../environments/environment.prod';

export const baseUrl = 'http://localhost:3000';
export const produtsUrl = baseUrl + '/products'
export const cartUrl = baseUrl + '/cart'